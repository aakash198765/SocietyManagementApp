<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.societymanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegistrationFormSubmitted"/>
        <activity android:name=".ComplaintSubmitted"/>
        <activity android:name=".ViewStatus"/>
        <activity android:name=".RuleBook"/>
        <activity android:name=".EventsNotices"/>
        <activity android:name=".ServiceRegisterationForm"/>
        <activity android:name=".LocalServices"/>
        <activity android:name=".RegisterComplaint"/>
        <activity android:name=".HelpDesk"/>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>